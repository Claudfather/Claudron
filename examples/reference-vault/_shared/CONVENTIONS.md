# Vault conventions

<!-- Filled-in twin of schema.CONVENTIONS_TEMPLATE (the init default) —
     keep the shared bullets in sync; the first bullet is the
     standing-facts example the template leaves to the vault owner. -->

- Timezone: America/New_York. Maintainer: chris.
- One file per topic; update or supersede, never duplicate.
- Wikilink related notes at write time: `[[Title]]`.
- Agent captures enter as `maturity: draft`; humans promote.
- Stale? Set `superseded_by` and `status: superseded` — never delete.
